export namespace AccountRibbon {
    // sample ribbon button click event handler
    export function onButtonClick(selectedItemId: string) {
        // do something with the selected item id
        console.log(`Button clicked for item with id: ${selectedItemId}`)
    }
}
